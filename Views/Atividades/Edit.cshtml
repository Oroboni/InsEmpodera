@model Empodera.Models.Atividades
@{
    ViewData["Title"] = "Atividade - Editar";
    ViewData["PageCSS"] = "activity-form";
    Layout = "_Layout";
    var eixos = ViewBag.EixosList as List<Empodera.Models.Eixo>;
}

@section Styles {
    
    <link rel="stylesheet" href="~/css/pages/fichaprimeirocontato.css" />
    <link rel="stylesheet" href="~/css/pages/site.css" />
    <link rel="stylesheet" href="~/css/pages/activity-form.css" />
}

<form asp-controller="Atividades" asp-action="Edit" method="post" class="main-form">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="IdAtividade" />

    <div class="modern-container">
        <!-- Header -->
        <div class="header-section">
            <h2>
                <a asp-controller="Atividades" asp-action="Index" class="text-primary">Atividades</a>
                <span class="separator">></span>
                <strong class="text-muted">Detalhes da Atividade</strong>
            </h2>
        </div>

        <!-- Comunidade -->
        <div class="input-card full-width">
            <div class="icon-box"><i class="fa-solid fa-city"></i></div>
            <div class="input-content">
                <label for="ComunidadeId">Comunidade <span class="text-danger">*</span></label>
                <select id="ComunidadeId" name="ComunidadeId" class="clean-input" asp-items="ViewBag.Comunidades" required>
                    <option value="">Selecione uma comunidade</option>
                </select>
                <span asp-validation-for="FkIdComunidade" class="text-danger"></span>
            </div>
        </div>

        <!-- Nome da Atividade -->
        <div class="input-card full-width">
            <div class="icon-box"><i class="fa-solid fa-pencil"></i></div>
            <div class="input-content">
                <label asp-for="Nome">Nome da Atividade <span class="text-danger">*</span></label>
                <input type="text" asp-for="Nome" class="clean-input" required
                       placeholder="Digite o nome da atividade" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
        </div>

        <!-- Eixos -->
        <div class="input-card full-width">
            <div class="icon-box"><i class="fa-solid fa-tags"></i></div>
            <div class="input-content">
                <label>Eixos <span class="text-danger">*</span></label>
                
                <!-- Container de Tags Selecionadas -->
                <div class="eixos-tag-container" id="tag-container"></div>

                <!-- Dropdown de Seleção -->
                <details class="multiselect-wrapper">
                    <summary>
                        <div class="multiselect-trigger">
                            <span class="multiselect-placeholder">Selecione os eixos</span>
                            <i class="fa-solid fa-chevron-down"></i>
                        </div>
                    </summary>
                    <div class="multiselect-panel" id="multiselect-panel">
                        <ul>
                            @if (eixos != null)
                            {
                                @foreach (var eixo in eixos)
                                {
                                    <li>
                                        <input type="checkbox" 
                                               name="EixosSelecionados" 
                                               value="@eixo.IdEixo" 
                                               id="eixo-@eixo.IdEixo"
                                               data-tag-name="@eixo.Nome"
                                               @(Model.AtividadesEixos.Any(ae => ae.FkIdEixo == eixo.IdEixo) ? "checked" : "")>
                                        <label for="eixo-@eixo.IdEixo">@eixo.Nome</label>
                                    </li>
                                }
                            }
                        </ul>
                    </div>
                </details>
            </div>
        </div>

        <!-- Descrição -->
        <div class="input-card full-width">
            <div class="icon-box"><i class="fa-solid fa-align-left"></i></div>
            <div class="input-content">
                <label asp-for="Descricao">Descrição</label>
                <textarea asp-for="Descricao" class="clean-input" rows="4"
                          placeholder="Descreva a atividade..."></textarea>
                <span asp-validation-for="Descricao" class="text-danger"></span>
            </div>
        </div>

        <!-- Informações do Sistema -->
        @if (Model.IdAtividade > 0)
        {
            <div class="purple-divider"></div>
            <div class="section-label">Informações do Sistema</div>

            <div class="input-card full-width">
                <div class="icon-box"><i class="fa-solid fa-user-edit"></i></div>
                <div class="input-content">
                    <label>Criado por</label>
                    <div class="system-info-display">
                        <span class="info-user">
                            <i class="fa-solid fa-user"></i>
                            @ViewBag.UsuarioOriginal?.Nome
                        </span>
                    </div>
                </div>
            </div>

            <div class="input-card full-width">
                <div class="icon-box"><i class="fa-solid fa-calendar-plus"></i></div>
                <div class="input-content">
                    <label>Data de Criação</label>
                    <div class="system-info-display">
                        <span class="info-date">@Model.DtCriacao.ToString("dd/MM/yyyy HH:mm")</span>
                    </div>
                </div>
            </div>

            @if (ViewBag.UsuarioNovo != null)
            {
                <div class="input-card full-width">
                    <div class="icon-box"><i class="fa-solid fa-calendar-day"></i></div>
                    <div class="input-content">
                        <label>Última Atualização</label>
                        <div class="system-info-display">
                            <span class="info-date">@Model.DtModificacao.ToString("dd/MM/yyyy HH:mm")</span>
                            <span class="info-user">
                                <i class="fa-solid fa-user"></i>
                                @ViewBag.UsuarioNovo.Nome
                            </span>
                        </div>
                    </div>
                </div>
            }
        }

        <!-- Botões de Ação -->
        <div class="buttons-container">
            <a asp-controller="Atividades" asp-action="Index" class="wizard-btn btn-prev">
                <i class="fa-solid fa-arrow-left"></i> Voltar
            </a>

            <div class="action-buttons-group">
                <button type="button" class="wizard-btn" id="edit-save-btn">
                    <i class="fa-solid fa-edit"></i> Editar
                </button>

                @if (Model.IdAtividade > 0)
                {
                    <button type="button" class="wizard-btn btn-delete" id="openDeleteModalBtn">
                        <i class="fa-solid fa-trash"></i> Deletar
                    </button>
                }
            </div>
        </div>
    </div>
</form>

<!-- Modal de Confirmação de Exclusão -->
@if (Model.IdAtividade > 0)
{
    <div class="modal-overlay" id="deleteConfirmationModal">
        <div class="modal-content">
            <div class="modal-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h3 class="modal-title">Confirmar Exclusão</h3>
            <p class="modal-text">
                Você tem certeza que deseja deletar a atividade <strong>@Model.Nome</strong>?
                <br><br>
                Essa ação não pode ser desfeita.
            </p>

            <div class="modal-actions">
                <button type="button" class="btn-modal-cancel wizard-btn btn-prev" id="cancelDeleteBtn">
                    Cancelar
                </button>

                <form asp-controller="Atividades" asp-action="Delete" method="post" asp-route-id="@Model.IdAtividade">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn-modal-confirm wizard-btn btn-delete">
                        Sim, Deletar
                    </button>
                </form>
            </div>
        </div>
    </div>
}

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const form = document.querySelector('.main-form');
            const inputFields = form.querySelectorAll('.clean-input, textarea');
            const editSaveBtn = document.getElementById('edit-save-btn');
            const tagContainer = document.getElementById('tag-container');
            const multiselectPanel = document.getElementById('multiselect-panel');
            const checkboxes = multiselectPanel.querySelectorAll('input[type="checkbox"]');
            
            let isEditMode = false;
            let isSubmitting = false;

            // Configurar estado dos campos
            function setPageState() {
                if (isEditMode) {
                    inputFields.forEach(field => field.disabled = false);
                    checkboxes.forEach(cb => cb.disabled = false);
                    editSaveBtn.innerHTML = '<i class="fa-solid fa-check"></i> Salvar Alterações';
                    editSaveBtn.setAttribute('type', 'submit');
                    editSaveBtn.classList.add('btn-save-final');
                    editSaveBtn.classList.remove('btn-next');
                } else {
                    inputFields.forEach(field => field.disabled = true);
                    checkboxes.forEach(cb => cb.disabled = true);
                    editSaveBtn.innerHTML = '<i class="fa-solid fa-edit"></i> Editar';
                    editSaveBtn.setAttribute('type', 'button');
                    editSaveBtn.classList.add('btn-next');
                    editSaveBtn.classList.remove('btn-save-final');
                }
            }

            setPageState();

            // Função para atualizar as tags
            function updateTagView() {
                tagContainer.innerHTML = '';

                checkboxes.forEach(cb => {
                    const listItem = cb.closest('li');

                    if (cb.checked) {
                        const tagName = cb.dataset.tagName;
                        const tag = document.createElement('span');
                        tag.className = 'tag-item tag-purple';
                        tag.textContent = tagName;

                        const removeBtn = document.createElement('i');
                        removeBtn.className = 'fa-solid fa-xmark tag-remove-btn';
                        removeBtn.dataset.checkboxId = cb.id;

                        tag.appendChild(removeBtn);
                        tagContainer.appendChild(tag);

                        listItem.classList.add('is-selected');
                    } else {
                        listItem.classList.remove('is-selected');
                    }
                });
            }

            // Evento de mudança nos checkboxes
            multiselectPanel.addEventListener('change', function (e) {
                if (e.target.type === 'checkbox') {
                    updateTagView();
                }
            });

            // Evento de clique no X das tags
            tagContainer.addEventListener('click', function (e) {
                if (e.target.classList.contains('tag-remove-btn')) {
                    const checkboxId = e.target.dataset.checkboxId;
                    const checkboxToUncheck = document.getElementById(checkboxId);

                    if (checkboxToUncheck && !checkboxToUncheck.disabled) {
                        checkboxToUncheck.checked = false;
                        updateTagView();
                    }
                }
            });

            // Inicialização
            updateTagView();

            // Handler do botão editar/salvar
            editSaveBtn.addEventListener('click', function (e) {
                if (editSaveBtn.getAttribute('type') === 'submit') {
                    if (!isSubmitting && form.checkValidity()) {
                        isSubmitting = true;
                        editSaveBtn.disabled = true;
                        editSaveBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Processando...';
                        form.submit();
                    }
                } else {
                    e.preventDefault();
                    isEditMode = true;
                    setPageState();
                }
            });

            // Lógica do modal de exclusão
            const deleteModal = document.getElementById('deleteConfirmationModal');
            const openDeleteBtn = document.getElementById('openDeleteModalBtn');
            const cancelDeleteBtn = document.getElementById('cancelDeleteBtn');

            if (openDeleteBtn) {
                openDeleteBtn.addEventListener('click', function (e) {
                    e.preventDefault();
                    deleteModal.style.display = 'flex';
                    setTimeout(() => deleteModal.classList.add('active'), 10);
                });
            }

            if (cancelDeleteBtn) {
                cancelDeleteBtn.addEventListener('click', function () {
                    closeDeleteModal();
                });
            }

            window.addEventListener('click', function (e) {
                if (e.target === deleteModal) {
                    closeDeleteModal();
                }
            });

            document.addEventListener('keydown', function (e) {
                if (e.key === 'Escape' && deleteModal && deleteModal.classList.contains('active')) {
                    closeDeleteModal();
                }
            });

            function closeDeleteModal() {
                deleteModal.classList.remove('active');
                setTimeout(() => deleteModal.style.display = 'none', 300);
            }
        });
    </script>
}