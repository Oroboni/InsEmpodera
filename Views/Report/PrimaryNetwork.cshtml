@{
    ViewData["Title"] = "Relatório - Rede Primária";
    Layout = "_Layout";
    // A linha "DisableMainScroll" FOI REMOVIDA daqui para CORRIGIR a rolagem.
    
    // Pega o AtorId selecionado (enviado pelo Controller)
    int? selectedAtorId = ViewBag.SelectedAtorId;
}
@section Styles {
    <link rel="stylesheet" href="~/css/pages/reports.css" />
}

<div class="report-page-container">
    <div class="report-breadcrumb">
        <h1>Relatórios > <strong>Rede Primária</strong></h1>
    </div>

    <div class="report-content-box">
        <div class="report-filters">
            
            <form asp-action="PrimaryNetwork" method="get" id="atorSelectForm">
                <div class="form-group">
                    <label>Ator</label>
                    <div class="select-wrapper">
                        <i class="fa-solid fa-user"></i>
                        <select name="atorId" asp-items="ViewBag.AtorList" onchange="this.form.submit()">
                            <option value="">Selecione um Ator...</option>
                        </select>
                        <i class="fa-solid fa-chevron-down select-arrow"></i>
                    </div>
                </div>
            </form>

        </div>

        <div class="report-body">
            @if (selectedAtorId == null)
            {
                <div class="report-empty-state">
                    <img src="~/img/undraw_selection.svg" alt="Selecione um ator" />
                    <p>Primeiro selecione um ator.</p>
                </div>
            }
            else
            {
                <div class="metric-card-row">
                    <div class="metric-card">
                        <div class="title">Amplitude</div>
                        <div class="value">2</div>
                    </div>
                    <div class="metric-card">
                        <div class="title">Densidade</div>
                        <div class="value">50.00%</div>
                    </div>
                    <div class="metric-card">
                        <div class="title">Centralidade</div>
                        <div class="value" style="font-size: var(--font-size-base); color: var(--color-text-secondary); padding-top: 10px;">Nenhum item encontrado</div>
                    </div>
                </div>

                <div style="display: grid; grid-template-columns: 1fr 2fr; gap: 24px; align-items: center;">
                    <div class="chart-container" style="max-width: 250px;">
                        <canvas id="networkDonutChart"></canvas>
                    </div>
                    <div class="chart-container-bar">
                        <canvas id="networkBarChart"></canvas>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/reports.js"></script>
}