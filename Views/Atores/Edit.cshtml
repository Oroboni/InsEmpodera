@model Empodera.Models.Atores
@{
    ViewData["Title"] = "Detalhes do Ator";
    Layout = "_Layout";
}

@section Styles {
    <link rel="stylesheet" href="@Url.Content("~/css/site.css")" />
}

<div class="modern-container">
    <div class="header-section">
        <h2>
            <a asp-controller="Atores" asp-action="Index" class="text-primary">Atores</a>
            <span class="separator"> > </span>
            <strong class="text-muted">Detalhes do Ator</strong>
        </h2>
    </div>

    <form asp-action="Edit" method="post" class="main-form">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="IdAtores" />

        <div class="section-label">Informações Básicas</div>

        <div class="input-card full-width">
            <div class="icon-box purple"><i class="fa-solid fa-city"></i></div>
            <div class="input-content">
                <label for="ComunidadeId">Comunidade <span class="text-danger">*</span></label>
                <select id="ComunidadeId" name="ComunidadeId" class="clean-input simple-select"
                    asp-items="ViewBag.Comunidades" required disabled>
                    <option value="">Selecione a comunidade</option>
                </select>
                <i class="fa-solid fa-chevron-down dropdown-icon"></i>
            </div>
        </div>

        <div class="input-card full-width">
            <div class="icon-box purple"><i class="fa-solid fa-user"></i></div>
            <div class="input-content">
                <label asp-for="Nome">Nome <span class="text-danger">*</span></label>
                <input asp-for="Nome" type="text" class="clean-input" placeholder="Nome do ator" required disabled>
            </div>
        </div>

        <div class="purple-divider"></div>
        <div class="section-label">Dados Pessoais</div>

        <div class="grid-row">
            <div class="input-card">
                <div class="icon-box purple"><i class="fa-solid fa-calendar-days"></i></div>
                <div class="input-content">
                    <label for="Idade">Idade</label>
                    <input asp-for="Idade" type="number" class="clean-input" disabled>
                </div>
            </div>

            <div class="input-card">
                <div class="icon-box purple"><i class="fa-solid fa-venus-mars"></i></div>
                <div class="input-content">
                    <label asp-for="Genero">Gênero</label>
                    <select asp-for="Genero" class="clean-input simple-select" disabled>
                        <option value="">Selecione</option>
                        <option value="2">Feminino</option>
                        <option value="1">Masculino</option>
                        <option value="">Não declarado</option>
                        <option value="3">Transgênero</option>
                    </select>
                    <i class="fa-solid fa-chevron-down dropdown-icon"></i>
                </div>
            </div>
        </div>

        <div class="input-card full-width checkbox-card">
            <div class="input-content">
                <label for="da-equipe" class="checkbox-inline-label">
                    <input type="checkbox" id="da-equipe" name="da-equipe" asp-for="DaEquipe" disabled>
                    Da Equipe
                </label>
            </div>
        </div>

        <div class="grid-row">
            <div class="input-card">
                <div class="icon-box purple"><i class="fa-solid fa-people-group"></i></div>
                <div class="input-content">
                    <label asp-for="PapelSocial1">Papel Social</label>
                    <input asp-for="PapelSocial1" type="text" class="clean-input" placeholder="Papel social principal"
                        disabled>
                </div>
            </div>

            <div class="input-card">
                <div class="icon-box purple"><i class="fa-solid fa-heart"></i></div>
                <div class="input-content">
                    <label asp-for="PapelSocial2">Interesses</label>
                    <input asp-for="PapelSocial2" type="text" class="clean-input" placeholder="Interesses do ator"
                        disabled>
                </div>
            </div>
        </div>

        <div class="input-card full-width">
            <div class="icon-box purple"><i class="fa-solid fa-phone"></i></div>
            <div class="input-content">
                <label asp-for="Telefone">Telefone</label>
                <input asp-for="Telefone" id="inputTelefone" name="telefone" type="tel" class="clean-input"
                    placeholder="(00) 00000-0000" disabled>
            </div>
        </div>

        <div class="purple-divider"></div>
        <div class="section-label">Ações Relacionadas</div>

        <div class="actions-grid">
            <a asp-controller="PersonalAssessment" asp-action="Index" asp-route-atorId="@Model.IdAtores" class="action-card">
                <span class="card-title">Avaliações</span>
                <span class="card-subtitle">Pessoais</span>
                <div class="card-icons">
                    <i class="fa-solid fa-pen"></i>
                </div>
            </a>
            <a asp-controller="PersonalProcess" asp-action="Index" asp-route-atorId="@Model.IdAtores" class="action-card">
                <span class="card-title">Diário de</span>
                <span class="card-subtitle">Processos Pessoal</span>
                <div class="card-icons">
                    <i class="fa-solid fa-book"></i>
                </div>
            </a>
        </div>

        <div class="purple-divider"></div>
        <div class="section-label">Informações do Sistema</div>

        @if (Model.DtCriacao > DateTime.MinValue && ViewBag.UsuarioOriginal != null)
        {
            <div class="input-card full-width">
                <div class="icon-box purple"><i class="fa-solid fa-calendar-plus"></i></div>
                <div class="input-content">
                    <label>Criação</label>
                    <div class="user-info-display">
                        <i class="fa-solid fa-user"></i>
                        <span class="user-name">@ViewBag.UsuarioOriginal.Nome</span>
                        <span class="user-date">@Model.DtCriacao.ToString("dd/MM/yyyy HH:mm")</span>
                    </div>
                </div>
            </div>
        }

        @if (Model.DtModificacao > DateTime.MinValue)
        {
            <div class="input-card full-width">
                <div class="icon-box purple"><i class="fa-solid fa-user-edit"></i></div>
                <div class="input-content">
                    <label>Última atualização</label>
                    <div class="user-info-display">
                        <i class="fa-solid fa-user"></i>
                        <span class="user-name">@Context.Session.GetString("Nome")</span>
                        <span class="user-date">@Model.DtModificacao.ToString("dd/MM/yyyy HH:mm")</span>
                    </div>
                </div>
            </div>
        }

        <!-- Botões de Ação -->
        <div class="buttons-container">
            <a asp-controller="Atores" asp-action="Index" class="wizard-btn btn-prev">
                <i class="fa-solid fa-arrow-left"></i> Voltar
            </a>

            <div class="action-buttons-group">
                <button type="button" class="wizard-btn btn-next" id="edit-save-btn">
                    <i class="fa-solid fa-edit"></i> Editar
                </button>

                <button type="button" class="wizard-btn btn-delete" id="openDeleteModalBtn">
                    <i class="fa-solid fa-trash"></i> Deletar
                </button>
            </div>
        </div>

    </form>
</div>

<!-- Modal de Confirmação de Exclusão -->
<div class="modal-overlay" id="deleteConfirmationModal">
    <div class="modal-content">
        <div class="modal-icon">
            <i class="fas fa-exclamation-triangle"></i>
        </div>

        <h3 class="modal-title">Confirmar Exclusão</h3>

        <p class="modal-text">
            Você tem certeza que deseja deletar o Ator <strong>@Model.Nome</strong>?<br>
            Essa ação não pode ser desfeita.
        </p>

        <div class="button-container">
            <div class="modal-actions">
                <button type="button" class="wizard-btn btn-prev" id="cancelDeleteBtn">
                    Cancelar
                </button>

                <form asp-controller="Atores" asp-action="Delete" method="post" asp-route-id="@Model.IdAtores">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn-modal-confirm wizard-btn btn-next">
                        Sim, Deletar
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const form = document.querySelector('.main-form');
        const inputFields = form.querySelectorAll('.clean-input, select.clean-input, input[type="checkbox"]');
        const editSaveBtn = document.getElementById('edit-save-btn');

        // Variáveis de controle
        let isEditMode = false;
        let isSubmitting = false;

        // Configurar estado dos campos
        function setPageState() {
            if (isEditMode) {
                // Modo edição ativo
                inputFields.forEach(field => {
                    // Não habilitar campos readonly (metadados)
                    if (!field.hasAttribute('readonly')) {
                        field.disabled = false;
                    }
                });
                editSaveBtn.innerHTML = '<i class="fa-solid fa-check"></i> Salvar Alterações';
                editSaveBtn.setAttribute('type', 'submit');
                editSaveBtn.classList.remove('btn-next');
                editSaveBtn.classList.add('btn-save-final');
            } else {
                // Modo visualização
                inputFields.forEach(field => field.disabled = true);
                editSaveBtn.innerHTML = '<i class="fa-solid fa-edit"></i> Editar';
                editSaveBtn.setAttribute('type', 'button');
                editSaveBtn.classList.remove('btn-save-final');
                editSaveBtn.classList.add('btn-next');
            }
        }

        // Inicializar estado (visualização)
        setPageState();

        // Handler do botão editar/salvar
        editSaveBtn.addEventListener('click', function (e) {
            if (editSaveBtn.getAttribute('type') === 'submit') {
                // Se for submit, o navegador faz a validação HTML5 automática
                if (!isSubmitting && form.checkValidity()) {
                    isSubmitting = true;
                    editSaveBtn.disabled = true;
                    editSaveBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Processando...';
                    form.submit();
                }
            } else {
                // Alternar para modo edição
                e.preventDefault();
                isEditMode = true;
                setPageState();
                // Focar no primeiro campo editável
                const firstInput = form.querySelector('input:not([disabled]):not([type="hidden"]):not([readonly])');
                if (firstInput) firstInput.focus();
            }
        });

        // Lógica do modal de exclusão
        const deleteModal = document.getElementById('deleteConfirmationModal');
        const openDeleteBtn = document.getElementById('openDeleteModalBtn');
        const cancelDeleteBtn = document.getElementById('cancelDeleteBtn');

        if (openDeleteBtn) {
            openDeleteBtn.addEventListener('click', function (e) {
                e.preventDefault();
                deleteModal.style.display = 'flex';
                setTimeout(() => {
                    deleteModal.classList.add('active');
                }, 10);
            });
        }

        if (cancelDeleteBtn) {
            cancelDeleteBtn.addEventListener('click', function () {
                closeDeleteModal();
            });
        }

        // Fechar modal ao clicar fora
        window.addEventListener('click', function (e) {
            if (e.target === deleteModal) {
                closeDeleteModal();
            }
        });

        // Fechar modal com ESC
        document.addEventListener('keydown', function (e) {
            if (e.key === 'Escape' && deleteModal.classList.contains('active')) {
                closeDeleteModal();
            }
        });

        function closeDeleteModal() {
            deleteModal.classList.remove('active');
            setTimeout(() => {
                deleteModal.style.display = 'none';
            }, 300);
        }

        // Validação em tempo real para campos obrigatórios
        const requiredFields = form.querySelectorAll('[required]');
        requiredFields.forEach(field => {
            field.addEventListener('blur', function () {
                if (!this.value || (typeof this.value === 'string' && !this.value.trim())) {
                    this.classList.add('invalid');
                } else {
                    this.classList.remove('invalid');
                }
            });
        });
    });
</script>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
        <script src="~/js/site.js" asp-append-version="true"></script>
    }
}
